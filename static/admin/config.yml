backend:
  name: "test-repo"

media-folder: static/assets
public_folder: assets

collections:
  - name: blog
    label: Blog
    folder: blog
    create: true
    fields:
      - { name: path, label: Path }
      - { name: date, label: Date, widget: date }
      - { name: date, title: Date }
      - { name: date, title: Date }
      - { name: body, label: Body, widget: markdown }
